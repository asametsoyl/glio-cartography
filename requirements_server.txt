# ================================================================
# Glio-Cartography — Backend Server Requirements
# All packages actually imported by the Python backend.
# CPU-only PyTorch keeps the packed env size manageable (~1.5 GB).
# ================================================================

# --- Web server ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
aiohttp>=3.9.0          # Async HTTP (used by some scvi/liana internals)
httpx>=0.27.0            # Modern sync/async HTTP client

# --- Logging ---
loguru>=0.7.0

# --- Deep learning (CPU-only to keep package size <2 GB) ---
# Switch to https://download.pytorch.org/whl/cu121 for CUDA locally
torch>=2.1.0 --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.16.0 --index-url https://download.pytorch.org/whl/cpu

# PyTorch Geometric + dependencies
torch-geometric>=2.4.0
torch-scatter
torch-sparse
torch-cluster

# --- Core single-cell analysis ---
scanpy>=1.9.6
anndata>=0.10.0
squidpy>=1.4.0
leidenalg
python-igraph

# --- Cell-cell communication ---
liana>=1.0.0             # L-R analysis (LIANA+)

# --- Spatial deconvolution ---
tangram-sc>=1.0.4
cell2location>=0.1.3
scvi-tools>=1.0.0

# --- Hyperparameter optimization ---
optuna>=3.0.0

# --- Data science ---
numpy
pandas
scipy
scikit-learn
scikit-image
statsmodels
networkx
seaborn
matplotlib

# --- Data formats ---
h5py
tables              # pytables (needed by h5py/pandas HDF5)
zarr
dask[dataframe]
pyarrow
pyyaml

# --- Bioinformatics utilities ---
pydeseq2
decoupler              # decoupler (correct PyPI name: decoupler)
gseapy>=1.0.4          # GSEA enrichment (used by pathway analysis)
adjustText
biopython>=1.83        # Seq utilities (used by ncbi fetch / bioservices)

# --- Pathway Enrichment (pathway_mapper.py) ---
# scipy, numpy, pandas already covered above
# pillow already covered below

# --- Drug / annotation APIs ---
bioservices>=1.11.0
mygene>=3.2.2
gprofiler-official

# --- Report generation ---
openai>=1.0.0
jinja2
fpdf2

# --- Visualization extras ---
plotly>=5.18.0         # Interactive plots used in notebooks and optional views
distinctipy>=1.3.4     # Perceptually distinct color palettes for cell types

# --- Utilities ---
setuptools<82.0
python-dotenv
rich
colorama
tqdm
requests
pillow>=10.0.0         # PIL (used by pathway_mapper KEGG image overlay)
psutil>=5.9.0          # System resource monitoring

